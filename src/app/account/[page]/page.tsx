import {notFound} from 'next/navigation';import Link from 'next/link';
const pages:Record<string,{title:string;description:string}>={cart:{title:'Shopping Cart',description:'Your selected parts and USD order total will appear here.'},garage:{title:'My Garage',description:'Save vehicle details to speed up fitment requests.'},profile:{title:'Customer Profile',description:'Manage contact details, quotes and support requests.'}};
export function generateStaticParams(){return Object.keys(pages).map(page=>({page}))}
export default async function Page({params}:{params:Promise<{page:string}>}){const {page}=await params;const item=pages[page];if(!item)notFound();return <><section className="banner"><div className="container"><div className="breadcrumbs">Home / Account / {item.title}</div><h1 className="title">{item.title}</h1><p>{item.description}</p></div></section><section className="section"><div className="container content"><p>No saved items yet.</p><Link className="btn" href="/auto-parts">BROWSE ALL PARTS</Link></div></section></>}
