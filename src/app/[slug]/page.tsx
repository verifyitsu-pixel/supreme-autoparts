import {notFound} from 'next/navigation';import {infoPages} from '@/lib/data';import {ContactForm} from '@/components/Forms';
export async function generateStaticParams(){return Object.keys(infoPages).map(slug=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=infoPages[slug];return p?{title:p.title,description:p.description}:{};}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=infoPages[slug];if(!p)notFound();return <><section className="banner"><div className="container"><div className="breadcrumbs">Home / {p.title}</div><h1 className="title">{p.title}</h1><p>{p.description}</p></div></section><section className="section"><div className="container content">{p.body.map((x,i)=><p key={i}>{x}</p>)}{slug==='about-us'&&<><h2>Need the right part?</h2><ContactForm/></>}</div></section></>}
