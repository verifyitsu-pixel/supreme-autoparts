import Link from 'next/link';import {Truck,ShieldCheck,RefreshCcw,Clock,Tag,CheckCircle} from 'lucide-react';import {VehicleForm} from '@/components/Forms';import {categories,makes} from '@/lib/data';
export default function Home(){const feats=[[Truck,"Shipping's on Us",'Free initial shipping on qualifying orders.'],[RefreshCcw,'Easy Replacements','Clear support when a replacement is needed.'],[CheckCircle,'Reliability','Quality used OEM parts from trusted suppliers.'],[ShieldCheck,'Warranty','Standard warranty on engines and transmissions.'],[Clock,'Timely Delivery','Responsive fulfillment and tracking updates.'],[Tag,'Competitive Pricing','Multiple options designed to fit your budget.']] as const;return <><section className="hero"><div className="heroInner"><span className="eyebrow">Quality used OEM components</span><h1>Move forward with us</h1><p>Your car. Your choice. Our parts. Find dependable engines, transmissions and auto parts for nearly every major make.</p><Link className="btn" href="/auto-parts">SHOP PARTS</Link></div></section><div className="vehicle"><div className="container"><VehicleForm/></div></div><section className="section"><div className="container"><span className="eyebrow">Nationwide inventory</span><h2 className="title">Featured categories</h2><div className="grid">{categories.map(c=><Link className="card" href={`/products/${c.slug}`} key={c.slug}><div className="cardArt">{c.icon}</div><div className="cardBody"><h3>{c.name}</h3><p>{c.text}</p><span className="link">GO SHOP →</span></div></Link>)}</div></div></section><section className="section features"><div className="container"><span className="eyebrow">Why choose us</span><h2 className="title">Parts you can count on</h2><div className="featuresGrid">{feats.map(([Icon,t,d])=><div className="feature" key={t}><Icon size={34}/><h3>{t}</h3><p>{d}</p></div>)}</div></div></section><section className="section"><div className="container"><span className="eyebrow">Shop by brand</span><h2 className="title">Used auto parts for your make</h2><div className="makes">{makes.map(m=><Link className="chip" href={`/make/${m.toLowerCase().replaceAll(' ','-')}`} key={m}>{m}</Link>)}</div></div></section></>}
