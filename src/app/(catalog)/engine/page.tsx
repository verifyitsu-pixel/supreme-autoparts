import {makes} from '@/lib/data';import Link from 'next/link';import {VehicleForm} from '@/components/Forms';export const metadata={title:'Used Engines',description:'Shop gasoline, diesel and hybrid used OEM engines.'};export default function Page(){const types=[['Gasoline','High power output with widely available service and maintenance.'],['Diesel','Efficient, durable performance with strong low-end torque.'],['Hybrid','Fuel-efficient powertrains combining an engine and electric motor.']];return <><section className="banner"><div className="container"><div className="breadcrumbs">Home / Engine</div><h1 className="title">Used Engines</h1></div></section><div className="vehicle"><div className="container"><VehicleForm/></div></div><section className="section"><div className="container"><h2 className="title">Used engine types</h2><div className="grid">{types.map(([t,d])=><div className="card" key={t}><div className="cardArt">⚙️</div><div className="cardBody"><h3>{t}</h3><p>{d}</p><Link className="link" href={`/products/${t.toLowerCase()}-engine`}>SHOP →</Link></div></div>)}</div><h2 className="title" style={{marginTop:70}}>Engines for your make</h2><div className="makes">{makes.map(m=><Link className="chip" href={`/make/${m.toLowerCase().replaceAll(' ','-')}?part=engine`} key={m}>{m}</Link>)}</div></div></section></>}
