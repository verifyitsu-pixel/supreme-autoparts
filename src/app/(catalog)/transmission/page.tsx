import {makes} from '@/lib/data';import Link from 'next/link';import {VehicleForm} from '@/components/Forms';export const metadata={title:'Used Transmissions',description:'Shop automatic, manual, CVT and electric used OEM transmissions.'};export default function Page(){const types=['Manual Transmission','Automatic Transmission','Continuously Variable Transmission (CVT)','Automated Manual Transmission (AMT)','Electric Transmission'];return <><section className="banner"><div className="container"><div className="breadcrumbs">Home / Transmission</div><h1 className="title">Used Transmissions</h1></div></section><div className="vehicle"><div className="container"><VehicleForm/></div></div><section className="section"><div className="container"><h2 className="title">Transmission types</h2><div className="grid">{types.map(t=><div className="card" key={t}><div className="cardArt">🔩</div><div className="cardBody"><h3>{t}</h3><p>Quality tested OEM assemblies matched to your vehicle specifications.</p><Link className="link" href={`/products/${t.toLowerCase().replaceAll(' ','-')}`}>SHOP →</Link></div></div>)}</div><h2 className="title" style={{marginTop:70}}>Transmissions for your make</h2><div className="makes">{makes.map(m=><Link className="chip" href={`/make/${m.toLowerCase().replaceAll(' ','-')}?part=transmission`} key={m}>{m}</Link>)}</div></div></section></>}
