import {ContactForm} from '@/components/Forms';import Link from 'next/link';
import {allProducts} from '@/lib/catalog';
export function generateStaticParams(){return allProducts.map(product=>({slug:product.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const name=slug.replaceAll('-',' ');return{title:`${name} for sale`,description:`Request a quality ${name} matched to your vehicle.`}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=allProducts.find(product=>product.slug===slug);const name=item?.name||slug.replaceAll('-',' ');const price=item?.woo?.priceUsd;return <><section className="banner"><div className="container"><div className="breadcrumbs">Home / Auto Parts / {name}</div><h1 className="title">{name}</h1><p>Fitment verified | Kenya-wide delivery | Warranty options</p></div></section><section className="section"><div className="container"><div className="grid" style={{gridTemplateColumns:'repeat(auto-fit,minmax(280px,1fr))'}}><div className="cardArt" style={{height:420}}>AUTO PART</div><div><span className="eyebrow">USD pricing</span><h2 className="title">Find your {name}</h2><p><strong>Price: {price!=null?new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(price):'Request a USD quote'}</strong></p><p>Inventory changes daily. Send your year, make, model, VIN and preferred specification for exact options and final pricing.</p><ul><li>Fitment review</li><li>Quality supplier network</li><li>Tracking and support</li><li>Warranty options stated before purchase</li></ul><Link className="btn" href="#quote">GET A QUOTE</Link></div></div><div id="quote" style={{marginTop:70}}><h2 className="title">Request a quote</h2><ContactForm kind="quote"/></div></div></section></>}
