import {categories} from '@/lib/data';import Link from 'next/link';import {VehicleForm} from '@/components/Forms';export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const name=slug.split('-').map(x=>x[0].toUpperCase()+x.slice(1)).join(' ');return <><section className="banner"><div className="container"><div className="breadcrumbs">Home / Makes / {name}</div><h1 className="title">Used {name} Auto Parts</h1></div></section><div className="vehicle"><div className="container"><VehicleForm/></div></div><section className="section"><div className="container"><h2 className="title">Popular {name} parts</h2><div className="grid">{categories.map(c=><Link className="card" href={`/products/${slug}-${c.slug}`} key={c.slug}><div className="cardArt">{c.icon}</div><div className="cardBody"><h3>{name} {c.name}</h3><p>{c.text}</p></div></Link>)}</div></div></section></>}
