import type {MetadataRoute} from 'next';import {makes,categories,infoPages} from '@/lib/data';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://supremeautoparts.co.ke';const routes=['','/auto-parts','/engine','/transmission','/vehicle-search','/contact-us','/track-order','/submit-ticket','/make-payment','/testimonials','/faq','/blog',...Object.keys(infoPages).map(x=>`/${x}`),...makes.map(x=>`/make/${x.toLowerCase().replaceAll(' ','-')}`),...categories.map(x=>`/products/${x.slug}`)];return routes.map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:url===''?'weekly':'monthly',priority:url===''?1:.7}))}
