import Link from 'next/link';import {categories} from '@/lib/data';import {VehicleForm} from '@/components/Forms';export const metadata={title:'Used Auto Parts',description:'Browse quality used OEM auto parts and request exact vehicle fitment.'};
export default function Page(){const groups=['Air & Fuel','Axles','Brakes','Cooling & Heating','Doors','Electrical','Engine Accessories','Front Body','Glass & Mirrors','Interior','Lights','Rear Body','Safety','Suspension & Steering','Wheels'];return <><section className="banner"><div className="container"><div className="breadcrumbs">Home / Auto Parts</div><h1 className="title">Used Auto Parts</h1><p>Quality recycled OEM components for cars, trucks and SUVs.</p></div></section><div className="vehicle"><div className="container"><VehicleForm/></div></div><section className="section"><div className="container"><h2 className="title">Browse categories</h2><div className="grid">{[...categories,...groups.map((name,i)=>({name,slug:name.toLowerCase().replaceAll(' ','-').replace('&','and'),icon:['🔧','⚙️','🛞'][i%3],text:`Browse quality used OEM ${name.toLowerCase()} components.`}))].map(c=><Link className="card" href={`/products/${c.slug}`} key={c.slug}><div className="cardArt">{c.icon}</div><div className="cardBody"><h3>{c.name}</h3><span className="link">VIEW PARTS →</span></div></Link>)}</div></div></section></>}
