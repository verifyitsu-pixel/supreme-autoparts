import Link from 'next/link';import {productsByCategory,allProducts} from '@/lib/catalog';import {VehicleForm} from '@/components/Forms';export const metadata={title:'Auto Parts Catalog',description:'Browse the complete Supreme Autoparts product catalog.'};
export default function Page(){return <><section className="banner"><div className="container"><div className="breadcrumbs">Home / Auto Parts</div><h1 className="title">Complete Auto Parts Catalog</h1><p>{allProducts.length} product types across {productsByCategory.length} categories.</p></div></section><div className="vehicle"><div className="container"><VehicleForm/></div></div><section className="section"><div className="container"><h2 className="title">All categories</h2><div className="grid">{productsByCategory.map(group=><Link className="card" href={`/auto-parts/${group.slug}`} key={group.slug}><div className="cardArt">PARTS</div><div className="cardBody"><h3>{group.name}</h3><p>{group.products.length} products</p><span className="link">VIEW ALL PRODUCTS</span></div></Link>)}</div></div></section></>}
