'use client';import {useState} from 'react';
export function VehicleForm(){const [sent,setSent]=useState(false);return <form className="vehicleBox" onSubmit={e=>{e.preventDefault();setSent(true)}}><strong>SHOP BY VEHICLE</strong>{['Year','Make','Model','Part'].map(x=><div className="field" key={x}><label>{x.toUpperCase()}</label><select required defaultValue=""><option value="" disabled>Select {x}</option><option>Any {x}</option></select></div>)}<button className="btn">FIND PARTS</button>{sent&&<div className="notice" style={{gridColumn:'1/-1'}}>Great — we’ll show matching inventory. For an exact match, request a quote.</div>}</form>}
export function ContactForm({kind='quote'}:{kind?:string}){const [sent,setSent]=useState(false);if(sent)return <div className="notice">Thanks! Your {kind} request was received. Our parts team will contact you shortly.</div>;return <form className="form" onSubmit={e=>{e.preventDefault();setSent(true)}}><div className="formGrid"><div className="field"><label>NAME</label><input required minLength={2}/></div><div className="field"><label>EMAIL</label><input type="email" required/></div><div className="field"><label>PHONE</label><input type="tel" required/></div><div className="field"><label>ORDER / VIN (OPTIONAL)</label><input/></div></div><div className="field"><label>HOW CAN WE HELP?</label><textarea rows={6} required minLength={10}/></div><label style={{fontSize:13}}><input type="checkbox" required/> I agree to be contacted about this request.</label><button className="btn" type="submit">SUBMIT REQUEST</button></form>}
